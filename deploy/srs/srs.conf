# AnyLive 本地开发用最小 SRS 配置。
#
# 推流流名 = media/publish 签发的 HMAC 签名 key：
#   {room_id}_{exp}_{sig}  （不是裸房间 UUID）
# 播放/HLS 在签发后使用同一流名（API active stream 映射）。
#
# HTTP hooks：OBS 开始/停止推流时回调控制面 API。
# API 校验签名流名；若配置了 SRS_WEBHOOK_SECRET 则校验请求头。
listen              1935;
max_connections     1000;
daemon              off;
srs_log_tank        console;

http_api {
    enabled         on;
    listen          1985;
}

http_server {
    enabled         on;
    listen          8080;
    dir             ./objs/nginx/html;
}

vhost __defaultVhost__ {
    hls {
        enabled         on;
        hls_fragment    2;
        hls_window      10;
        hls_path        ./objs/nginx/html;
        hls_m3u8_file   [app]/stream].m3u8;
        hls_ts_file     [app]/stream]-[seq].ts;
    }
    http_remux {
        enabled     on;
        mount       [vhost]/app]/stream].flv;
    }

    # Point at the API (host network / docker host). Override for prod.
    # host.docker.internal works on Docker Desktop; Linux may need
    # extra_hosts or host gateway IP instead.
    http_hooks {
        enabled         on;
        on_publish      http://host.docker.internal:8088/api/v1/webhooks/srs/on_publish;
        on_unpublish    http://host.docker.internal:8088/api/v1/webhooks/srs/on_unpublish;
    }
}
